# Implementation Plan: S2.7 — Final Verification, Version Bump, CHANGELOG, Tag

**Story:** S2.7 (last of E02-rename-roughly, P0, never parallelized)
**Spec source:** [docs/planning/epics/E02-rename-roughly.md](../planning/epics/E02-rename-roughly.md) L565–663
**Branch:** `feat/S02.7-final-verification`
**Today:** 2026-04-30

## Summary

S2.7 finalizes the rename release: fix two residue audit violations surfaced during discovery (S2.5 oversights), bump the plugin version, finalize the CHANGELOG date heading, run the full static residue audit against the allow-list, and create the local `v0.1.4` tag. Push is deferred per human preference (GitHub repo rename happens first). Manual scratch-directory upgrade tests (steps 5, 5b, 6, 6b, 7 of the spec) are human-owned and run separately before the tag is pushed.

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `docs/adrs/README.md` | Edit | T1 |
| `docs/adrs/ADR-004-ui-conditional-not-forked.md` | Edit | T1 |
| `.claude-plugin/plugin.json` | Edit | T2 |
| `CHANGELOG.md` | Edit | T3 |

## Tasks

### T1: Fix two audit violations (PV-1 + PV-2) (~2 min)

**Files:**
- `docs/adrs/README.md`
- `docs/adrs/ADR-004-ui-conditional-not-forked.md`

**Action:** Replace the remaining capital-R `Ruckus` body-text occurrences that fall outside the S2.7 allow-list at epic L591 (which permits capital-R `Ruckus` only at ADR-005 L11, ADR-006 L11, ADR-008 L11).

**Details:**

1. Edit `docs/adrs/README.md` L3:
   - Old: `"ADRs capture the reasoning behind significant design decisions in Ruckus."` (exact text — verify by reading L1–L5 first)
   - New: `"ADRs capture the reasoning behind significant design decisions in Roughly."`
   - This is a single-word substitution: `Ruckus` → `Roughly`.

2. Edit `docs/adrs/ADR-004-ui-conditional-not-forked.md` L41:
   - Old: contains substring `"not bundled with Ruckus"` (read L40–L42 to confirm exact wording before editing)
   - New: replace `Ruckus` → `Roughly` on that line only.
   - Do NOT touch L11, L17, or L27 of this file — those are explicitly allow-listed for inline `/ruckus:*` namespace tokens (not capital-R prose).
   - Do NOT touch the v0.1.4 footnote at the bottom of the file — those `ruckus` mentions are allow-listed per epic L590.

**Constraints:**
- Match case exactly: capital-R `Ruckus` → capital-R `Roughly`. Lowercase tokens are NOT in scope here.
- Do NOT edit any other line in either file.
- Do NOT touch ADR-005, ADR-006, ADR-008 (their capital-R `Ruckus` at L11 is explicitly allow-listed).

**Verify:**
```
rg -nw 'Ruckus' docs/adrs/README.md docs/adrs/ADR-004-ui-conditional-not-forked.md
```
Expected output: empty (zero matches). The grep flag `-w` matches whole word, so the lowercase `ruckus` mentions in v0.1.4 footnotes (allow-listed) and the `/ruckus:*` namespace tokens at L11/L17/L27 of ADR-004 (also allow-listed) will NOT match this command — only capital-R `Ruckus` standalone words would. After T1, both files should produce zero hits for this exact command.

**UI:** no

---

### T2: Bump plugin version to 0.1.4 (~1 min)

**Files:** `.claude-plugin/plugin.json`

**Depends on:** none (independent of T1)

**Action:** Bump the `version` field from `"0.1.3"` to `"0.1.4"`.

**Details:**

1. Read `.claude-plugin/plugin.json` to confirm current state. Expect `"version": "0.1.3"` at L4.
2. Replace exactly that string with `"version": "0.1.4"`. Preserve indentation and surrounding JSON formatting.
3. Do NOT modify any other field in the file (`name`, `description`, etc. are already correct from S2.1).

**Verify:**
```
jq -r '.version' .claude-plugin/plugin.json
```
Expected output: `0.1.4` (literal, on its own line).

**UI:** no

---

### T3: Finalize CHANGELOG v0.1.4 date heading (~1 min)

**Files:** `CHANGELOG.md`

**Depends on:** none (independent of T1, T2)

**Action:** Replace the placeholder text in the v0.1.4 entry heading with today's release date.

**Details:**

1. Read `CHANGELOG.md` L1–L5 to confirm current state. Expect L3 to read:
   ```
   ## [0.1.4] — In Progress (E02 rename release)
   ```
2. Edit only L3. Replace it with:
   ```
   ## [0.1.4] — 2026-04-30
   ```
3. Do NOT modify any other line in the file. The body of the v0.1.4 entry (L4–L50, including the deliberate non-standard `### Migration` heading at L43) is complete from prior stories (S2.1–S2.6) and must be preserved verbatim per epic L644.
4. Do NOT touch the historical entries v0.1.0–v0.1.3 (L52 onward) — those retain their original "ruckus" naming as historical fact per epic L583.

**Verify:**
```
sed -n '3p' CHANGELOG.md
```
Expected output: `## [0.1.4] — 2026-04-30`

**UI:** no

---

## Blast Radius

**Do NOT modify outside this plan:**
- Any other line in any ADR (allow-listed body text and footnotes must be preserved)
- The v0.1.4 entry body in CHANGELOG.md L4–L50
- Historical CHANGELOG entries v0.1.0–v0.1.3
- README.md (S2.5 work; the Migration subsection at L57–L70 is allow-listed)
- Any skill, agent, or template file
- `marketplace.json` (no version field there per discovery)
- `docs/plans/**` (historical, allow-listed)

**Watch for:**
- Accidental whole-line replacements when a single-word substitution is intended
- Editing the wrong ADR (only ADR-004 README and the ADR index README are in T1's scope)
- Trailing-whitespace changes that could surface as noise in code review
- Re-running tasks after a partial completion — each verify command is idempotent, but Edit tool calls require an exact `old_string` match

## Conventions

- **Allow-list authority:** Epic L582–597 enumerates every allowed `ruckus`/`Ruckus` match. Anything outside that list is a violation.
- **Hard-cut release per ADR-001 / ADR-007 spirit:** No backwards-compatibility shims; the rename is total except for explicitly-preserved historical text.
- **CHANGELOG `### Migration` heading** is deliberately non-standard (not Keep-a-Changelog convention) — preserve verbatim per epic L644.
- **No commits between tag-create and tag-push** per epic L646. Wrap-up stage handles the tag after the consolidated commit.

## Final Audit (Stage 7 / orchestrator-driven, not a task)

After T1–T3 complete and the per-task verify commands all pass, the orchestrator will run the full S2.7 static residue audit in Stage 7 (verify-all) to satisfy AC L651:

```
rg -i ruckus
```

Expected: every remaining match falls into one of the allow-list categories at epic L582–597. Discovery already classified the current state — after T1 fixes PV-1 and PV-2, the only remaining `Ruckus`/`ruckus` matches are allow-listed historical, migration-context, footnote, and ADR-body-text references.

Additional Stage 7 spot-checks:
- `rg -n '/roughly:build-ui'` returns zero matches (ADR-004 compliance, AC L656)
- `git tag -l v0.1.4` returns empty pre-commit (AC L663 setup)

## Manual Tests (human-owned, NOT executed by this pipeline)

These ACs cannot be automated by a subagent. The human runs them in scratch directories before pushing the tag:

- **AC L654:** Plugin loads in fresh Claude Code session (`claude --plugin-dir <repo>`)
- **AC L655:** All 9 slash commands resolve under `/roughly:*` namespace
- **AC L657:** End-to-end pipeline test (`/roughly:setup` + `/roughly:build` in a scratch dir)
- **AC L658:** v0.1.4 upgrade migration test (epic step 5)
- **AC L659:** v0.1.2 step regression test (epic step 5b)
- **AC L660:** Conflict-case test (epic step 6)
- **AC L661:** Partial-failure resume test (epic step 6b)
- **AC L662:** Customized-boilerplate warning test (epic step 7)

The wrap-up stage will surface this list to the human as a checklist before commit/tag.

## Tag (wrap-up, not a task)

After human approves the consolidated commit:

```
git tag v0.1.4
```

Per epic L646: tag is created locally only; no push. If a hotfix is needed before push, delete the tag (`git tag -d v0.1.4`), commit the fix, re-create the tag on the new HEAD.
